# -*- coding: utf-8 -*-
"""
video_rewash v7.0 — 视频去重冲洗工具（模块化架构）

核心链路: preset.py → randomizer.py → processor.py → ffmpeg_runner.py
处理流程: 探测 → 预设 → 参数快照 → 独立处理 → 标准化 → 质检 → (混剪 → 成品质检 → 轻量二次处理)

说明（设计原则，修正 v6 时期过强结论）：
- 全参数随机能显著扩大输出差异空间，但不能"保证"绕过任何平台的内容识别；
  文档中的通过率数字均为目标指标，需以固定测试集实测校准。
- 不同平台音频指纹算法鲁棒性差异大，音频变换只提高失效概率而非必然失效。
- 编码参数随机属于压缩域扰动，优先级低于画面内容变换。
- 重复帧/删帧/音画偏移有观感风险，默认保守且必须经过质量检测。
- "全参数开启"= 全部具备随机化能力，允许温和取值，避免画质损伤累积。
"""

__version__ = "7.0.0"
